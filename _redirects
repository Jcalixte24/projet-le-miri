# _redirects — Règles de redirection Netlify
# Ce fichier est lu automatiquement par Netlify

# ══════════════════════════════════════════════════════════════
# 🔒 SUSPENSION-ABONNEMENT — NE PAS SUPPRIMER CE COMMENTAIRE
# Abonnement client expiré depuis le 10/09/2026.
# Cette règle redirige TOUT le trafic (pages + images + JS/CSS du
# site) vers la page suspendu.html tant qu'elle est active.
#
# ➜ POUR RÉACTIVER LE SITE : supprimer uniquement les lignes
#   entre "SUSPENSION-START" et "SUSPENSION-END" ci-dessous,
#   puis commit + push (Netlify redéploie automatiquement).
# SUSPENSION-START
/admin       /admin/index.html    200!
/admin/*     /admin/index.html    200!
/cms.js      /cms.js               200!
/*           /suspendu.html       200!
# SUSPENSION-END
# ══════════════════════════════════════════════════════════════

# /admin sans slash → /admin/index.html
/admin    /admin/index.html    200

# Pages statiques dédiées (SEO multi-pages)
/contact                              /contact.html          200
/evenements                           /evenements.html       200
/administration                       /administration.html   200
/groupe-scolaire                      /groupe-scolaire.html  200
/enseignants                          /enseignants.html      200
/parents                              /parents.html          200
/vie-scolaire                         /vie-scolaire.html     200

# Fallback SPA — toutes les autres URLs inconnues
/*    /index.html    200
